1. Check agent pods:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "inspection-agent.name" . }}

2. View agent logs:
   kubectl logs -n {{ .Release.Namespace }} deploy/{{ include "inspection-agent.fullname" . }}
